<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yuncanting.dao.TestEntityDao" >
	<select id="queryOne" parameterType="string" resultType="java.util.Map">
        SELECT * FROM test WHERE name = #{name}
    </select>
	<select id="queryOnes" parameterType="string" resultType="java.util.Map">
        SELECT * FROM test WHERE name = #{name}
    </select>
</mapper>